[project]
name = "rural-sustainability-backend"
version = "0.1.0"
description = "鄉村永續與數位轉譯 - 後端應用"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]